<div class="{{typography.subheader}}">Что у вас<br>в медицинской карте?</div>
<div class="{{typography.paragraph}}">Есть сейчас или было когда-то.</div>
<form class="{{gameStyles.formDisease}}" data-action="test-form">
	<div class="{{gameStyles.formDiseaseContainer}}">
		{{#each data}}
			<div class="{{../gameStyles.checkbox}}">
				<input type="checkbox" data-action="test-input" name="game-auto-checkbox" id="game-auto-checkbox-{{@index}}" class="{{../inputsStyles.checkboxInput}}" data-id={{@index}}>
				<label for="game-auto-checkbox-{{@index}}" class="{{../inputsStyles.checkboxLabel}}" {{#if this.danger}}data-danger="true"{{/if}}>{{this.text}}</label>
			</div>
		{{/each}}
	</div>
	<div class="{{gameStyles.actions}}">
		<button class="{{inputsStyles.button}}" data-action="test-submit">дальше</button>
	</div>
</form>